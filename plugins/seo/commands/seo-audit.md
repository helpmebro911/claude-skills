---
description: Run a full SEO audit on a website
argument-hint: "<domain>"
allowed-tools: "*"
---

Load the `seo-audit` skill and run a full SEO audit on `$ARGUMENTS`.

If no domain provided, ask the user which site to audit.
